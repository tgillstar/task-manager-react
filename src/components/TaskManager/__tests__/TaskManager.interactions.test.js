import React from "react";
import { render, screen } from "@testing-library/react";
import TaskManager from "../TaskManager";

test("placeholder for interaction tests", () => {
    expect(true).toBe(true);
  });
  